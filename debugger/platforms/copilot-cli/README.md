# Copilot CLI Adaptation

This package aims to mirror the highest-value parts of the Code Buddy experience inside GitHub Copilot CLI plugin conventions.

Included:

- generated agents
- skill
- hooks
- MCP binding

Model routing defaults to `inherit` unless the host exposes finer-grained control.
