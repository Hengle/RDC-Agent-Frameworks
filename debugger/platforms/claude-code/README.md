# Claude Code Adaptation

This platform should be treated as a first-class target, not as a prompt mirror only.

Desired capability set:

- subagents
- hooks
- MCP
- model alias routing (`opus` / `sonnet`)
- optional local skill references

The generated `agents/` files are only one part of the adaptation.
