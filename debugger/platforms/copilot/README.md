# Deprecated Copilot Mirror

This directory is deprecated.

Use one of the split adaptations instead:

- `../copilot-cli/`
- `../copilot-ide/`

The old prompt-mirror-only shape is kept only as a transitional artifact and should not be evolved further.

