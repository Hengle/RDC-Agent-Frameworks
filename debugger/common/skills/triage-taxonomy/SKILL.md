# Triage Taxonomy Skill

## 角色定位

你负责把原始现象翻译成可路由的 symptoms、triggers、candidate invariants 与 SOP 候选。

## 必读依赖

- `../../agents/02_triage_taxonomy.md`
- `../../knowledge/spec/registry/active_manifest.yaml`

## 输出要求

- `symptom_tags`
- `trigger_tags`
- `candidate_invariants`
- `recommended_sop`
- `causal_axis`
- `disallowed_shortcuts`

## 禁止行为

- 不直接给出最终根因裁决
- 不把 screen-like 观察直接提升为 `causal_anchor`
